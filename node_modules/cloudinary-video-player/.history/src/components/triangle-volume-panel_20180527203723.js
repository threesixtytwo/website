import videojs from 'video.js';

// const VolumeMenuButton = videojs.getComponent('VolumeMenuButton');
const VolumePanel = videojs.getComponent('VolumePanel');
// const Popup = videojs.getComponent('Popup');

class TriangleVolumePanel extends VolumePanel {
  debugger; // eslint-disable-line
  // createPopup() {
  //   const popup = new Popup(this.player_, {
  //     contentElType: 'div'
  //   });

  //   const vb = new VolumeBar(this.player_, this.options_.volumeBar);

  //   popup.addChild(vb);

  //   this.menuContent = popup;
  //   this.volumeBar = vb;

  //   this.attachVolumeBarEvents();

  //   return popup;
  // }
}

videojs.registerComponent('triangleVolumePanel', TriangleVolumePanel);

export default TriangleVolumePanel;
